# READ FIRST

This repository contains the canonical operating context for TimDesign and Timtim's AI-assisted work.

Before starting any task, read:

1. `ai_context/os_index.md`

Then follow the read order in that file.

Core rule:

Use the OS before asking Timtim for context that may already be documented.

When useful durable knowledge appears, update the matching OS file.
