# Codex Starter

Use at the start of every Codex task connected to TimDesign_OS.

```text
Read `AGENTS.md` or `BOOTSTRAP.md` in the repo root before doing any work.

If both exist, treat `AGENTS.md` as the coding-agent entry point and `BOOTSTRAP.md` as the OS entry point.

Then read `ai_context/os_index.md` and follow its read order.

Before asking Timtim for context, check the relevant OS registry:
- `ai_context/knowledge_profile.md`
- `ai_context/tool_registry.md`
- `ai_context/project_registry.md`
- `ai_context/skills_registry.md`

Complete the task as far as possible, run relevant checks, and update the matching OS file when durable knowledge appears.

Keep explanations compact. Timtim is an advanced user in Webflow, web design, UX/UI, product strategy, AI tools, agents, MCPs, prompting, and freelance operations.
```

## Recommended repo-level setup

Create or maintain a root `AGENTS.md` that points Codex to `BOOTSTRAP.md` and `ai_context/os_index.md`.

## Notes

AGENTS.md is the interoperable context file format used by many coding agents, including Codex.
