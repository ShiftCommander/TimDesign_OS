# Universal Agent Starter

Use this at the beginning of a new ChatGPT, Codex, Antigravity, OpenCode, Gemini, or Cursor session connected to TimDesign_OS.

```text
Before doing anything else, read `BOOTSTRAP.md` in this repository.

Then follow the read order in `ai_context/os_index.md`.

Use the OS context before asking Timtim for information that may already be documented.

For this task:
1. identify the relevant project, tool, and skill entries;
2. use the documented workflow when available;
3. complete the task as far as possible;
4. update the relevant OS file if durable knowledge, a working setup, a failed setup, a fallback, or a project-state change is discovered.

Keep explanations compact. Timtim is advanced in web design, Webflow, UX/UI, product strategy, AI tools, coding agents, MCPs, prompting, and freelance operations.
```
